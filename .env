# ============================================
# Backend Environment Variables Template
# ============================================
# Copy this file to .env and fill in your actual values
# DO NOT commit .env to version control!

# ============================================
# Server Configuration
# ============================================
PORT=3001
HOST=localhost

# ============================================
# CORS Configuration
# ============================================
# Comma-separated list of allowed origins
# Example: http://localhost:5173,http://localhost:3000,http://3.110.108.83
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000,http://3.110.108.83

# ============================================
# MongoDB Configuration
# ============================================
# MongoDB connection string
# For MongoDB Atlas (cloud):
MONGODB_URI=mongodb+srv://testuser:JBkUSvgwKzaGNcV0@semicolon.xehtz.mongodb.net/
# For local MongoDB:
# MONGODB_URI=mongodb://localhost:27017/movies

# ============================================
# JWT Authentication Configuration
# ============================================
# Secret key for JWT token signing (CHANGE THIS IN PRODUCTION!)
# Generate a strong random string for production use
JWT_SECRET=222fed36629593d6a11d4f19daca0576
# Token expiration time (e.g., 24h, 7d, 1h)
JWT_EXPIRATION=24h

# ============================================
# AWS S3 Configuration
# ============================================
# AWS region where your S3 bucket is located
# Common regions: us-east-1, us-west-2, eu-west-1, ap-south-1
AWS_REGION=ap-south-1

# AWS Access Key ID
# Get this from AWS IAM Console ? Users ? Your User ? Security Credentials
AWS_ACCESS_KEY_ID=

# AWS Secret Access Key
# Get this from AWS IAM Console ? Users ? Your User ? Security Credentials
# IMPORTANT: Keep this secret! Never commit it to version control.
AWS_SECRET_ACCESS_KEY=

# S3 Bucket Name
# The name of your S3 bucket where images will be stored
AWS_S3_BUCKET_NAME=

# ============================================
# Notes:
# ============================================
# 1. Replace all placeholder values (your-xxx-here) with your actual credentials
# 2. For AWS S3 setup instructions, see S3_SETUP.md
# 3. Make sure your .env file is in .gitignore (it should be by default)
# 4. Never share your .env file or commit it to version control

